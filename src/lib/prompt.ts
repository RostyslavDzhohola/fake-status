export interface PromptPreset {
  scene: "yacht";
  guidance: string;
}

export const yachtPreset: PromptPreset = {
  scene: "yacht",
  guidance:
    "golden-hour rim light, shallow DOF, teak deck, chrome rail, turquoise water bokeh",
};
